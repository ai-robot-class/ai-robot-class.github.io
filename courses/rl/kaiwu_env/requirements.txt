# 开悟 1v1 CPU 环境的 Python 依赖
# PyTorch(CPU) 在 Dockerfile 中单独安装（使用官方 CPU 源）

# 开悟官方 SDK（新版）。注意 PyPI 包名是 `hok`（不是旧的 `hok_env`）。
# 使用 `hok.hok1v1` 接口；内含 py3.6~3.9 预编译的 interface 扩展，故 Python 必须 ≤ 3.9。
# 版本需与 gamecore 匹配：本包 gamecore 为 v45_1450123，对应 hok 45.x。
hok==45.1.5

# 与 gamecore-server 的 HTTP/通信、数值与工具库
requests
pyzmq
numpy
protobuf~=3.20
loguru
