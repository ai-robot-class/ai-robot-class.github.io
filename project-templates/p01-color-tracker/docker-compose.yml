services:
  dev:
    build: .
    image: airobot-class/p01-color-tracker:latest
    container_name: p01-color-tracker
    network_mode: host
    privileged: true
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - QT_X11_NO_MITSHM=1
      - ROS_DOMAIN_ID=42
    volumes:
      - .:/workspace
      - /tmp/.X11-unix:/tmp/.X11-unix
      - /dev/video0:/dev/video0
    devices:
      - /dev/video0
    stdin_open: true
    tty: true
    command: /bin/bash
